# 規約条文と実装の突合表

弁護士の指摘（2026-07-30 Q25）に基づく、**利用規約の全条項 × 実装 × テスト**の突合です。

> **この一件の本質は個別のバグではなく、規約条文と実装の対応関係が管理されて
> いないことです。** 公開前に一度、規約の全条項を1行ずつ「対応する実装・対応する
> テスト」に紐づける突合表を作ることを強く勧めます。**条文が実装より進んで
> いる状態（＝守れない約束）は、施行後は端的に債務不履行になります。**

- 作成: 2026-07-30
- 対象: `terms-of-service-draft.md`（第1条〜第16条）
- **サービスは未公開・規約は未施行**のため、現時点の不一致に実害はありません。
  **公開前に⚠️をすべて解消することが本表の目的です。**

## 凡例

| 記号 | 意味 |
|---|---|
| ✅ | 実装あり＋**テストで固定済み** |
| ◑ | 実装あり・テストなし |
| 📄 | 表示・運用で担保（コード実装を伴わない） |
| ℹ️ | 宣言的条項（実装を要しない） |
| ⚠️ | **条文が実装より進んでいる（守れない約束）→ 要対応** |

## 検証方法

- 実装の有無は**コードとDBを実際に確認**しました（推測では書いていません）
- 「テストで固定済み」は `supabase/tests/` に該当の検証があるものだけです
- **確認していない項目は「未検証」と明記**しています

---

# ⚠️ 見つかった不一致（5件）— **G4 以外は解消済み**

**うち2件は 2026-07-30 の条文改訂で当社が新たに作ってしまったものです。**

| # | 条項 | 内容 | 深刻度 |
|---|---|---|---|
| ~~**G1**~~ | 第8条1項 / 特商法表記 | ✅ **解消。**条文・特商法表記とも「最長10時間」に揃えた |
| ~~**G2**~~ | 第8条の2 3項 | ✅ **解消。**`fee_rates()` RPC ＋ `HostSettingsScreen`（`fetchFeeRates`）で段階制・3pt引き・下限・ギフト率を表示している |
| ~~**G3**~~ | 第8条の2 5項 | ✅ **解消（`0091`）。**料率に `effective_from` を持たせ、**予約の成立日時（`confirmed_at`）**の版で計算する |
| **G4** | 第3条3項 | **「日本国内に居住する個人に限る」と書いたが、居住地の申告・確認の仕組みが無い** ← 07-30に当社が作った | 中（弁護士は自己申告で足るとした） |
| ~~**G5**~~ | 第7条の3 5項（購入取消時はサポート料も返還） | ✅ **実装済み（`0090`）。**`admin_void_purchase()` が①未使用コインの取消し②コイン代金とサポート料を別々の返金債務に③本人へ通知、を1操作で行う。会計は前受金の取崩し（J25）と**売上のマイナス**（J26・税理士Q14(b)）に分かれる。チャージバックがある購入は `HAS_DISPUTE` で拒否（二重返金）。テスト `25_void_purchase.sql` |

> **教訓**: 条文を書くときに実装の有無を確認していませんでした。
> 今後、条文を追加・変更したら**この表を必ず同時に更新**します。

---

# ⚠️ 2026-08-05 の横断点検で見つかった不一致（6件）

**弁護士回答の反映後に、規約・特商法表記の全条項と実装を突き合わせ直したもの**です。
G1〜G5 とは別に、新たに6件見つかりました。**うち4件は、実装を変えたときに
条文を直し忘れたもの**で、G1 と同じ型の間違いを繰り返しています。

| # | 条項 | 内容 | 深刻度 | 状態 |
|---|---|---|---|---|
| **G16** | 第8条1項 / 特商法表記 | **「14日後まで」と書いているが、実装は35日**（`platform_pricing.max_lead_days = 35`／`MAX_LEAD_DAYS = 35`）。`0061`（まとめ予約）で28日先が入るよう延ばしたときに、条文を直していなかった | 中（表示の不正確） | ✅ **条文・特商法表記を35日に修正** |
| **G17** | 第8条 / 特商法表記 | **まとめ予約（同じ曜日・時刻を4回分まとめて申込み）が、規約にも特商法表記にも一切書かれていない。** 画面には出ており、コインも4回分まとめて消費する | **中〜高**（特商法の「役務の内容」「申込みの撤回・解除に関する事項」の表示） | ✅ **第8条1の2項を新設＋特商法表記に行を追加。弁護士確認事項として起票**（継続的役務提供に触れないか） |
| ~~**G18**~~ | 第9条5の2 後段 | 「**キャンセルの手続を行う画面において、返還されるコインの有効期限が近い場合はその旨を事前に表示します**」 | — | ✅ **実装済み（`0109`）。**`my_booking_refund_quote()` に「戻らない枚数／期限が近い枚数／最も早い期限／金銭返金になる枚数」を足し、`Talk.tsx` のキャンセル確認に出す。割当ては `_refund_lot_forecast()` が**実際の返還処理と同じ順序・同じ配分**でなぞる。テスト `31_refund_expiry_preview.sql`（**見積りと実際が一致することまで突き合わせている**） |
| ~~**G19**~~ | 第7条の3 / 0106 | 購入時の同意を記録していなかった（表示はしていた） | — | ✅ **実装済み。**`src/content/purchasePolicy.ts` を出典にし、購入ボタンの直前に**畳まずに**表示して、決済ページへ送る直前に同じ文面を `policy_consents` へ残す。テスト `30_dispute_evidence.sql`（**証跡に purchase の同意が出ること**まで固定） |
| ◑ **G20** | 第3条6項・7項（08-05新設） | 6項「同意の日時と版を記録します」／7項「推奨する動作環境を表示します」 | 小 | ✅ **6項は実装済み。**登録画面に規約・プライバシーポリシーへの同意チェックを置き（**チェックしないと登録できない**）、セッションが立った直後に `policy_consents` へ残す。**点検して分かったのは「記録していない」ではなく「同意の手続そのものが無かった」**こと。<br>✅ **7項も実装した**（2026-08-07）。`src/content/systemRequirements.ts` に置き、**未ログインで読める `/about`** に節を出す。設定画面の「規約・ポリシー」からも辿れる。数値は憶測で書かず実装から取った（下限は Vite 5 の既定ターゲット `modules`＝ES2020、iOS の通知は `src/lib/push.ts` の制約）。**ビルドターゲットや通知の仕組みを変えたら、ここも直すこと** |
| ✅ **G21** | 第10条の2 6項 | 「投稿等を削除することができます／**削除の理由をユーザーに通知します**」。**運営コンソールに投稿の削除機能そのものが無かった** | 小（削除しなければ通知義務も生じないが、DPF法の削除要請にSQL Editorで対応することになる） | ✅ **対応した**（2026-08-07・`0112`）。運営コンソールに「募集」タブを新設。`admin_remove_board_post` は**理由が必須**で、**投稿者に理由をそのまま通知**する（6項の通知義務はここで果たす）。参加表明していた人には理由を伏せて取り下げだけ伝える。行は消さず `status=cancelled` にして通報の裏取りに残し、`admin_actions` に記録する |

## ~~別件: ギフトの会計処理（規約と実装の食い違い）~~ → ✅ 解消（2026-08-05）

規約 第7条の2（08-04版）の構成は**総額処理**を含意していましたが、
`accounting_journal()` の実装は**純額**（売上に立つのは利用料35%だけ）でした。

税理士の回答（2026-08-05）:

> 「**規約が債権債務関係を定義し、それが実態を構成する**、が答えです。……
> 08-04構成（当社の自己債務）なら総額、**08-05構成（受領権限）なら純額**が
> 理論的帰結です。**実装が既に純額である以上、08-05への書き換えは
> 税務上も整合的で望ましい。**」

**第7条の2を追加対価＋受領権限構成に書き換えて解消しました。**
会計は元から純額なので、**条文のほうを実装に合わせた**形です。
ギフトが従たる地位に留まっているかは `admin_business_kpis` の
`giftToBookingPercent`（`0110`）で毎月見ます。

## 役務提供のガイドライン・事例集（2026-08-06）

| 層 | 文書 | 位置づけ | 到達 |
|---|---|---|---|
| 第1層 | `legal/service-standard-guideline.md` | **規約の一部**（第1条2項） | `/legal/guideline`・**未ログイン可**・設定画面 |
| 第2層 | `help/service-standard-examples.md` | 規約の外（ヘルプ） | `/legal/examples`・**未ログイン可**・設定画面 |
| 内規 | `help/service-standard-ops.md` | 社内 | アプリに出さない |

**08-06に3点を足しました**（2026-08-05の弁護士回答 論点D(b)）:
中核的要素⑤「共同プレイの実体」（約束の有無にかかわらない下限＝放置）／
時間の不足は按分が原則で全額は不提供時に限る旨／
原因不明の按分の原資は確定前の預かり分（**ピタメイトの報酬から控除しない**）。

**未ログインからの到達**は、規約 第3条6項で同意を求める以上の必須要件です
（読めない文書への同意にしない）。従前は設定画面からしか辿れませんでした。

## 点検して**ずれていなかった**もの（記録）

| 項目 | 規約 | 実装 |
|---|---|---|
| コインの有効期限 | 取得日から6か月を経過する日の前日 | `coin_expiry_from()` ✅ |
| 換金の最低額・手数料 | 5,000コイン／300コイン | `request_bank_payout` の定数 ✅ |
| 利用料の上限 | 予約30%・ギフト40%を超えない | `host_fee_tiers` / `gift_fee_rates` の CHECK 制約 ✅ |
| 現行の率 | 20/17/14/12%・3pt引き・下限10%・ギフト35% | テーブルの値と `_booking_fee_coins` ✅ |
| 初回お試し割引 | 0〜90%。**延長分は通常価格** | `extend_booking` は `hourly_rate` をそのまま使う ✅ |
| キャンセルの段階 | 5分以内／1時間前まで全額・以後50%・開始後0%・没収上限3時間 | `platform_pricing` の5値 ✅ |
| 自動確定 | 72時間。ゲストが24時間以上に短縮可 | `hours smallint check (hours between 24 and 72)` ✅ |
| 未応答の失効 | 一定時間内に応答しなければ全額返還 | `expire-stale-booking-requests`（毎時47分）✅ |
| 期限が近いコインの事前通知 | 第7条5の3 | `notify-expiring-coins`（0089）✅ **G7は解消済み** |
| 料率変更 | 30日前通知＋不利益変更は個別通知 | `admin_schedule_fee_change`（30日未満は拒否・全ホストへ通知）✅ |
| 申出の期間 | 完了確定から14日以内 | `claim_window_days = 14` ✅ |
| 新規ユーザーの購入上限・換金保留 | 30日・1万円/3万円・30日保留 | `platform_pricing` の4値 ✅ |
| チャージバック相殺の時的限界 | 報酬確定から180日 | `0099` ✅ |

---

# 突合表

## 第1条（適用）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 規約の適用 | ℹ️ | 宣言 |
| 2 | ガイドライン等が規約の一部を構成 | 📄 | `src/content/legalDocs.ts`（アプリ内表示） |

## 第2条（本サービスの目的と性質）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | ゲーム仲間を見つける目的 | ℹ️ | 宣言 |
| 2 | 異性交際・出会い目的ではない | 📄 | 規約第5条1号の禁止＋`contentGuard.ts` の `dating` カテゴリ検知 |
| 3 | 当社は役務提供の当事者ではない | ℹ️ | 宣言（収納代行の整理の基礎。第8条3項・4項と対応） |

## 第3条（登録・本人確認）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 利用には本人確認の完了が必要 | ◑ | `identity_verifications` + `approve_identity_verification`。**アプリの導線では必須**（signUp→consent→verify→setup）。ただし `create_booking` は**ホストの `is_verified` のみ検査**し、ゲスト側は検査していない（RPCを直接叩けば未確認でも予約できる） |
| 2 | 18歳未満は利用できない | ◑ | `identity_verifications.is_adult`（審査時に運営が判定）。自動的な年齢計算はしていない |
| 3 | **日本国内に居住する個人に限る** | ⚠️ **G4** | **実装なし。** 居住地の申告欄も確認もない。弁護士は「**自己申告で足りる**」としたので、登録時のチェックを足せば足りる |
| 4 | 真実かつ正確な情報の提供 | ℹ️ | 宣言（違反時は第6条） |
| 5 | 本人確認画像は照合後すみやかに削除 | ◑ | 運用（`docs/manual-verification-review.md`）。**自動削除は未実装** |

## 第4条（みまもり〔監視〕への同意）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | メッセージ等を確認することがある | ◑ | `src/lib/contentGuard.ts`（ルールベース検知） |
| 2 | **原則として自動処理**。人が見るのは通報時等に限る | ◑ | `contentGuard.ts` は**送信をブロックせず**、本人に確認ダイアログを出すのみ。運営の閲覧は通報経由（`admin_reports`。0068で閲覧を記録） |
| 3 | 対象はサービス内のやり取りに限る | ℹ️ | 宣言 |
| 4 | **送信はブロックしない／記録は一致断片のみ** | ✅ | `contentGuard.ts`（送信前の注意表示のみ）＋ `content_flags.matched`（0028。本文を保存しない設計で、表のコメントにもそう書いてある） |
| 5 | 個別同意を取得し、日時と説明の版を記録 | ✅ | `Consent.tsx`（専用画面・チェック必須）＋ `monitoring_consents`（0031） |
| 6 | **撤回できる。撤回すればやりとり機能が止まる。既存予約の履行と換金は続く** | ✅ | **0074**。`messages` / `invites` / `bookings` / `board_posts` / `board_participants` の BEFORE INSERT トリガで止め、update（チェックイン・完了・キャンセル）と換金は触らない。相手側の撤回でも止まる（Q19：メッセージは双方の通信）。画面は `Settings.tsx` の `MonitoringConsentRows`。テスト `92_monitoring_revoke.sql`（11項目。**止めすぎ・止め足らずを両側から固定している**） |

## 第5条（禁止事項）

| 号 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 異性交際・出会い目的 | ◑ | `contentGuard.ts` の `dating` 検知＋通報カテゴリ |
| 3 | 外部誘導・アプリ外の金銭要求 | ◑ | `contentGuard.ts` の `contact` / `money` 検知。通報カテゴリ `external_invite` |
| 2,4〜8 | わいせつ・ハラスメント・なりすまし等 | 📄 | 通報＋運営対応（`admin_reports` / `resolve_report`） |
| — | ボイスチャット併用の例外 | ℹ️ | 宣言（`contentGuard.ts` の許可リストで Discord 等を除外） |

## 第6条（利用制限・アカウント停止）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 警告・削除・停止・強制退会 | 📄 | 運営が手動で実施（専用RPCは未整備） |
| 2 | 理由の詳細を開示しないことがある | ℹ️ | 宣言 |
| 4 | 再審査の求め | 📄 | 問い合わせ窓口（メール） |
| 5 | **再登録防止のため本人確認情報の一部をハッシュ等で保持することがある** | ◑ | **実装なし。** `docs/trust-safety-spec.md` に運用方針として記載があるだけで、ハッシュを保持する仕組みは無い。条文は「することがある」（当社の権限）なので**守れない約束ではない**が、trust-safety-spec が掲げる「身分証ベースの再登録防止」は現状**実行できない** |

## 第7条（コインの購入・利用）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | コインの購入 | ✅ | `create-checkout-session` / `stripe-webhook` / `credit_coins_for_purchase`（0009） |
| 2 | 本サービス内での消費 | ✅ | `create_booking` / `send_gift` |
| 3 | **払い戻し・換金・出金はできない** | ✅ | 購入コイン（`balance`）に換金経路は無い。換金は `earned_balance` のみ（`request_bank_payout`） |
| 4 | 第三者への譲渡・貸与・売買の禁止 | ℹ️ | 宣言（ギフトは第7条の2の例外として別建て） |
| 5 | **有効期限は取得日から6か月を経過する日の前日まで** | ✅ | `coin_expiry_from`（0018）＋ `expire_coins()`。ロット単位で管理 |
| 5の2 | **消費順序（期限の早い順）** ★07-31追加 | ✅ | `_split_coins_by_expiry`（**0082**）。`tests/97` が固定。**購入ボーナスの廃止（`0083`）により、条文から有償/無償の区別を削除** |
| 5の3 | 期限が近いコインの事前通知・残高と有効期限の表示 ★07-31追加 | ◑ | ✅ **実装済み（`0089`）。**`notify_expiring_coins()` を毎日9:09に実行（期限14日前に1通だけ・同じロットは二度送らない）＋ `Wallet.tsx` に「有効期限ごとの残高」。**G7は解消**（08-05の点検で確認） |
| 6 | 報酬コインの換金（最低5,000・手数料300・毎週日曜締め翌週金曜払い） | ✅ | `request_bank_payout`（0063 + **0069**）。`tests/89` で保留の境界を固定 |
| 7 | **報酬コインは換金専用（消費に充てられない・購入不可・譲渡不可・期限なし）** ★07-31追加 | ✅ | 元から `earned_balance` は消費経路から分離。`tests/89` が非失効を固定。**条文が実装に追いついた形** |

## 第7条の2（ありがとうギフト）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | **実際に一緒に遊んだ（予約が完了した）相手にのみ** | ✅ | `send_gift` の `NO_COMPLETED_PLAY`（0020〜0022）。**`tests/89` で固定**（弁護士Q24の最重要防壁） |
| 2 | 原資は購入コイン（有償分）に限る | ✅ | `send_gift` は `balance` のみを引く（0016の有償先消費） |
| 3 | 取消し・返金・返還ができない | ℹ️ | 宣言（取消RPCを作っていない） |
| 5 | **受領日から7日間は換金できない** | ✅ | `request_bank_payout` の `GIFT_ON_HOLD`（**0069で復活**）。`tests/89` で境界を両側から固定 |
| 6 | 上限・相互送金の禁止・チャージ後の制限・不自然な送受信の検知 | ✅ | `send_gift`（0020: 1回/日/月の上限・`MUTUAL_GIFT_FORBIDDEN`・`RECENT_PURCHASE_COOLDOWN`）＋0021（端末）＋0022（IP）＋0097（受領側30日・同一相手30日・完了からの期間）。**上限の数値は 0101 で `platform_pricing` へ移し、運営コンソール「制限値」から変更できる**（条文が「一定期間」としか書いていないため）。天井は CHECK 制約が持つ |
| 7 | **濫用時の没収** | ◑ | **自動処理は未実装。** 運営の個別判断による処分を想定（条文は「講じることができる」なので守れない約束ではない）。会計処理は税務Q13で確認中 |
| 8 | 税金は受領者の負担 | ℹ️ | 宣言 |

## 第7条の3（あんしんサポート料）★2026-07-30 全面改訂

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | コイン価格に加えて徴収・**区分して表示** | ✅ | `safety_fee_for`（0035）。購入画面に内訳＋総額、Stripe Checkoutで**別明細**。`tests/90` で売上計上を検証 |
| 2 | **対価となる役務の列挙**（本人確認・みまもり・通報対応・トラブルの事実確認・問い合わせ窓口） | ◑ | 5つのうち①本人確認 ②みまもり ③通報の受付は実装あり。④トラブルの事実確認・当事者への連絡 ⑤問い合わせ窓口は**運用**（メール） |
| 3 | コインに変換されず残高に加算されない | ✅ | `safety_fee_yen` は `coin_purchases` に別列。付与は `coins_credited` のみ |
| 4 | コインの返還が生じてもサポート料は返還しない | ℹ️ | 宣言（返金処理を作っていないので現状は自動的に満たされる） |
| 5 | **購入自体が取り消される場合はサポート料も返還する** | ⚠️ **G5** | **返金処理そのものが未実装。** Stripeの `charge.dispute` / `refund` を受ける処理がない（税務Q14でも同じ点を確認中） |
| 6 | 料率・金額は消費税を含む | 📄 | 表示のみ。税理士Q9で確認中 |

## 第8条（ピタメイト機能）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | ホストが**自ら設定した**時給レート | ✅ | `host_settings.hourly_rate`（50〜2,000コイン/時）。**ホスト本人が変更**（収納代行の要件④。税理士も確認） |
| 1 | 開始時刻の指定（30分後〜14日後） | ✅ | `platform_pricing.min_lead_minutes` / `max_lead_days`（0040） |
| 1 | **1件あたり最長4時間** | ⚠️ **G1** | **実装は最長10時間**（`max_duration_minutes = 600`。0049で設定）。画面も14択で10時間まで選べる。**条文と特商法表記が古い** |
| 1 | 諾否の自由・承諾で成立 | ✅ | `approve_booking`（0017） |
| 1 | 未応答なら全額返還 | ✅ | `expire_stale_booking_requests`（0017/0018/0030） |
| 2 | 提供するのはゲームを一緒に遊ぶ時間のみ | ℹ️ | 宣言（🔒欄の一線と対応） |
| 3 | 合意はユーザー間で成立 | ℹ️ | 宣言 |
| 4 | **受領権限の授与** | ℹ️ | 宣言（弁護士の修正案A-1〜A-3を反映予定） |
| 5 | 税務申告はホストの責任 | 📄 | 規約への明記＋（予定）マイページの年間支払明細 |
| 6 | **雇用関係・指揮命令関係がない** ★07-30新設 | ℹ️ | 宣言。実態は🔒欄の禁止事項（稼働誘導・ペナルティ・金額連動ランキングを作らない）で担保 |

## 第8条の2（プラットフォーム利用料）★2026-07-30 改訂

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | ホストが利用料を支払う | ✅ | `platform_fees`（0033/0036） |
| 2 | **相殺構成**（全額引き渡し債務と利用料債務を相殺） ★07-30 | ℹ️ | 条文上の構成。実装は差額を `earned_balance` に加算する形で、**結果は一致** |
| 3 | **具体的な率は本サービス上に表示します** ★07-30 | ⚠️ **G2** | **画面に料率を表示していない。** `host_fee_tiers` に率はあり（`authenticated` が読める）が、取得関数も表示も無い |
| 4 | 変更は30日前までに通知 ★07-30 | 📄 | 運用 |
| 5 | **変更前に成立した予約には変更前の率を適用** ★07-30 | ✅ | **実装済み（`0091`）。**`host_fee_tiers.effective_from` と `gift_fee_rates` で料率を版管理し、手数料は**予約の成立日時**の版で計算する。上限30%/40%はCHECK制約。変更は `admin_schedule_fee_change()` だけを通り、**30日以上先＋理由必須＋全ピタメイトへ個別通知**（第4項）。テスト `26_fee_grandfathering.sql` |
| 6 | 同一ゲストの2回目以降は3pt引き（下限10%） | ✅ | 0036/0060。`tests/40_host_fees.sql` |
| 7 | 直前キャンセル分には利用料を控除しない | ✅ | 0015/0016（機会損失の補償として全額をホストへ） |
| 7の2 | **プレイ開始後のキャンセルは完了と同じ扱い**（利用料を控除し、ゲストの不利益に扱わない） | ✅ | `cancel_booking` が両者のチェックイン（0050）を見て `_booking_fee_coins` を適用（0102）。`tests/26` が「完了とキャンセルで控除額が同じ」ことを固定 |
| 2 | 利用料は**受け取った対価から**控除する | ✅ | `_apply_booking_fee` の基準を「予約の全額」から「実際に付与された額（`booking_earned` の合計）」に修正（**0103**）。一部返還つきの保留解除で、受け取っていない分にまで課金していた（50%返還で実効40%・全額返還でも他の予約の残高から控除）。`tests/27` が固定 |

## 第8条の3（延長）／第8条の4（初回お試し割引）

| 条項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 8の3 | 進行中の延長・追加コイン | ✅ | `extend_booking`（0039。延長は定価） |
| 8の3 2項 | 延長分に初回割引は適用しない | ✅ | 0039（`tests/60_extension.sql` は**意図的に失敗する文で終わる**ため一括実行から除外） |
| 8の4 | 初回お試し割引（0〜90%） | ✅ | `host_trial_discount_for`（0038）。`tests/90_trial_discount.sql` |

## 第9条（キャンセルとコインの返還）

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 段階的な返還（5分以内/1時間前/50%/0%） | ✅ | `platform_pricing.cancel_grace_minutes` / `free_cancel_hours` / `late_cancel_refund_percent`（0040）。`tests/95_scheduled_cancel.sql` |
| 2 | 没収分はホストの報酬に | ✅ | 0015/0016 |
| 3 | 無断欠席の自動処理 | ✅ | 0050。`tests/89_no_show_auto.sql` |
| 4 | 内容が著しく異なる場合の相談 | 📄 | 問い合わせ窓口＋`admin_release_hold_*`（0066） |
| 5 | 障害時のコイン復元 | 📄 | 運用（`integrity_checks` 0043 で検知） |
| **5の2** | **返還時に期限切れなら消滅・画面で事前表示** ★07-30 | ◑ | 期限の引き継ぎは実装済み（0030）。**画面の事前表示は未実装** |
| 6 | 72時間の自動確定 / **24時間への短縮設定** | ✅ | `auto_complete_bookings`（0015/0042/0048/0062）＋**0072（非遡及）**。`tests/77` `tests/91` |
| 6 | 保留中は自動確定しない | ✅ | 0042（`held_at`）。`tests/97_booking_hold.sql` |

## 第8条の6（購入の効力が失われた場合の清算）★2026-07-31 新設

| 項 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 1 | 本人認証手続への協力義務 | ✅ | 3DS2 を `create-checkout-session` で要求（`request_three_d_secure`） |
| 2 | 購入の失効＝コインは遡って付与されなかったものとする | ℹ️ | 宣言 |
| 3 | 充当された部分に限り、**未払の**報酬コインから控除 | ◑ | `0075`（凍結）・`0077`（係争中の換金保留）まで。**ロット単位の充当トレースは未実装** |
| 4-1 | 対象は当該失効した購入から充当された分に限る | ⬜ | **未実装**（論点8(b)②。相殺対象の特定に要る） |
| 4-2 | 未払残高に対してのみ | ✅ | `request_bank_payout` は未払分しか触らない |
| 4-3 | 事前の通知と異議の機会 | ⬜ | 運用（未整備） |
| 4-4 | 3DS成立分は対象外 | ⬜ | 内規（未整備） |
| 5-1 | 新規ユーザーの購入上限 | ✅ | `purchase_limit_status`（0087）。**数値は運営コンソール「制限値」から変更できる**（0101。条文が数値を書いていないため規約改定にならない） |
| 5-2 | リスクベースの換金保留（最長30日） | ✅ | `payouts.hold_until`（0088）。日数は同じく「制限値」から。**天井30日は CHECK 制約**（条文が「最長30日間」と明記しているため）|
| 5-3 | 異議申立て中のコイン消費の停止 | ✅ | `0075`。`tests/93` で固定 |

## 第10条〜第16条

| 条 | 内容 | 状態 | 実装 / 根拠 |
|---|---|---|---|
| 10 | マナースコア等の表示 | ✅ | `profile_trust_stats` + `src/lib/trustDisplay.ts`（実績が少ないうちは数値を出さない） |
| 11 | 未成年者による利用 | 📄 | 第3条2項の年齢確認＋自主返金の運用方針 |
| 12 | 個人情報の取扱い | 📄 | プライバシーポリシー |
| 13-1 | 変更・中断・終了 | ℹ️ | 宣言 |
| 13-2〜5 | **終了時の取扱い（90日前告知・有償コインの金銭返金・報酬コインの手数料なし換金）** ★07-31新設 | ⬜ | **未実装。**終了は手作業で行う想定だが、**手順書が無い** |
| 14 | 免責（**賠償上限は直近1か月の支払額と1万円の高い方**）★07-31改訂 | ℹ️ | 宣言 |
| 15 | 規約の変更 | 📄 | 運用（`legalDocs.ts` の更新） |
| 16 | 準拠法・管轄 | ℹ️ | 宣言（第3条3項の国内限定の根拠のひとつ） |

---

# 対応計画

| # | 対応 | 状態 |
|---|---|---|
| **G1** | 規約 第8条1項と特商法表記を**「最長10時間」に修正** | ✅ **本コミットで修正** |
| **G2** | 料率を画面に表示する（取得関数＋表示） | ✅ **本コミットで実装** |
| ~~**G3**~~ | 料率に有効期間を持たせ、予約成立時点の率を適用する | ✅ **完了（`0091`）** |
| **G4** | 登録時に居住地の自己申告を追加 | ✅ **0081で実装**（本人確認の前に申告を求め、記録する） |
| **G5** | 返金・チャージバックの処理（Stripeイベント受信） | ◑ `0075`/`0077` で受信・凍結・保留まで。**第8条の6の相殺は未実装** |
| — | 第4条 撤回導線（E-5） | ✅ **0074で実装**（条文4項・6項も同時に追記） |
| — | 第9条5の2 期限の事前表示 | ⬜ 未着手 |
| ~~**G6**~~ | 第6条の2（退会）★07-31新設 | ✅ **実装済み（`0086`）。**設定→退会画面で消滅コイン数を事前表示、成立済み予約があれば実行不可、報酬コインは退会後90日だけ換金可、期限後は `expire-withdrawn-earned` で消滅。テスト `21_account_withdrawal.sql` |
| ~~**G7**~~ | 第7条5の3（失効の事前通知・残高と有効期限の表示）★07-31新設 | ✅ **実装済み（`0089`）。**`notify-expiring-coins`（日次）が期限14日前に1通だけ通知（同じロットは二度送らない）。ウォレットに期限ごとの残高を表示。テスト `24_expiry_notice_claim_window.sql` |
| ~~**G8**~~ | 第9条5の3（ゲスト無帰責の返還で消滅する分の金銭返金）★07-31新設 | ✅ **実装済み（`0085`）。**`cash_refunds` に返金債務を立て、運営が支払を実行。返還の入口6か所すべてに事由を明示。テスト `13_cash_refund_lapsed.sql` |
| ~~**G9**~~ | 第9条4項（申出は完了確定から14日以内）★07-31新設 | ✅ **実装済み（`0089`）。**`hold_booking(..., 'claim')` が期限切れなら `CLAIM_WINDOW_CLOSED`。確定前は制限なし。運営の職権(`manual`)は対象外。押す前に `claim_window_status()` で可否が分かる |
| ~~**G10**~~ | 第13条2〜5項（終了時の返金・換金）★07-31新設 | ✅ **手順書を作成（`docs/service-termination-runbook.md`）。**告知90日前／有効期限内コインの金銭返金／最低額・手数料を外した換金／会計の締め。**未実装の3点（一斉告知・免除・一括起票）も明記** |
| **G10-2** | 第13条8項（申出期間経過後の扱い）★08-05新設 | ◑ **手順書に反映済み**（`service-termination-runbook.md`「同業の実例」）。条文は**弁護士確認待ち**（`lawyer-questions-2026-08-04.md` 論点F）。実装は終了を決めてからで足りる |
| **G10-3** | 第13条3項ただし書（無償付与コインは返金対象外）★08-05新設 | ✅ **確認済み。** 「購入付随の増量分は無償に含まない」を追記（08-05回答 論点F(d)）。実装は `0083` の CHECK（`bonus_coins = 0`）で増量を発行できないため、区分と条文が一致している |
| **G12** | 第6条の2第4項（退会後の報酬コインを消滅させない）★08-05改訂 | ✅ **実装済み（`0107`）。**cron が毎日 `earned_balance = 0` にしていたのをやめ、**窓口を閉じるだけ**にした。個別の申出は運営コンソールの換金タブから起票（`admin_closed_window_balances` / `admin_payout_on_request`）。テスト `21_account_withdrawal.sql` §8・§8の2 |
| **G13** | 第6条の3第5項（利用停止時の購入コインを一律没収しない）★08-05改訂 | ✅ **実装済み（`0108`）。**`admin_suspend_account` に `p_forfeit_paid`（既定 false）を足し、**不正関連のときだけ**消す。消さなかった分は第7条5項の有効期限で通常どおり失効。テスト `24_suspension_and_withdrawal_payout.sql` §4 |
| **G14** | 第3条6項（規約への同意の日時と版を記録）★08-05新設 | ⬜ **未実装。** 現状は同意を記録していない。`policy_consents`（0106）に kind='terms' があるので、登録フローから呼べば足りる |
| **G15** | 第6条の4（長期未利用アカウント）★08-05新設 | ⬜ **未実装。** 2年の判定と30日前通知。**公開直後に効く条項ではない**ので急がない |
| ~~**G11**~~ | 第8条の6（相殺の対象特定・上限・通知／新規上限／リスク保留）★07-31新設 | ✅ **実装済み（`0087`+`0088`）。**第5項1号=購入上限（登録30日/未確認/異議ありで1回10,000円・30日30,000円）、第3項・第4項=`chargeback_offsets` で通知→異議7日→運営実行、控除は利用料控除後の未払報酬が上限、起点は dispute が lost の購入だけ（4項4号）、第5項2号=`payouts.hold_until` で振込のみ最長30日保留。テスト `22_purchase_limit.sql` / `23_chargeback_offset.sql`。**運営コンソールのUIは未実装（SQLから実行）** |
| — | 第3条1項 `create_booking` でゲストの本人確認も検査 | ⬜ 未着手（アプリ導線では必須のため実害は小さい） |
| — | 第5条7号 対面・飲食の禁止 | ✅ **2026-07-30 追加**（税理士Q22-c。宣言的条項で実装は不要） |
| — | 第8条1項 単価の上下限の趣旨 | ✅ **2026-07-30 追加**（税理士Q11-a）。実装は `host_settings` の 50〜2,000 制約 |
| — | 第8条の5 消費税と税率改定 | ◑ 条文は追加。**税率改定の運用は未整備**（改定時は第15条の手続が要る） |
| — | 第8条の5 4項 適格請求書発行事業者でない旨 | ✅ 事実として正しい。`host_settings.invoice_registration_number` は**未使用のまま維持** |

## この表の運用ルール

**条文を追加・変更したら、この表を同時に更新すること。**
今回、07-30の条文改訂で⚠️を4件（G2〜G5）作ってしまったのは、
条文を書くときに実装の有無を確認していなかったためです。

> ⚠️ **2026-07-31の改訂で、未実装の条項が6件（G6〜G11）増えました。**
> 弁護士（セカンドオピニオン）の指摘に沿って条文を先に整えたためで、
> **意図的に作った「条文が実装より進んだ状態」です。**
>
> **公開前に、この6件をすべて ✅ にするか、条文の側を落とす必要があります。**
> 施行後にこの状態を残すと、端的に債務不履行になります。

---

## 2026-08-01 追補: 同種サービス（GameRoom）の規約との突合

GameRoom（株式会社ゲームトレード運営／2025-12-15改定）の規約全文と条文単位で突合し、
**当社に無い条項を7つ**、**真似してはいけない条項を7つ**特定しました。

### 当社に無かったもの → 追加した条項

| 追加した条項 | 何が無かったか | 実装 |
|---|---|---|
| **第3条の2**（アカウントの管理） | ID・パスワードの管理責任と、なりすまし利用の帰属が無かった。カード共有の監視（E-9）まで実装しているのに責任の分界が無い状態 | 実装不要（責任分界の定め） |
| **第10条の2**（投稿等の取扱い） | ★**投稿物に関する条文が1つも無かった。** プロフィール文・音声・アバター・**レビュー**を投稿させ、第10条1項で「表示することがあります」と約束しているのに、**表示する根拠が無い**状態 | ✅ **`0092`**。4項の「退会後は将来に向かって終了」を守るため、退会時に bio / voice_path / avatar_path / favorite_games の参照を落とす。テスト `21_account_withdrawal.sql` |
| **第11条3項**（成年到達後の追認） | 未成年者取消しの発動場面を減らす実務条項が無かった | 実装不要 |
| **第16条の2**（通知・連絡） | 退会後90日の換金案内・料率変更の個別通知を送る設計なのに、到達みなしの受け皿が無かった | 実装不要 |
| **第16条の3**（権利義務の譲渡・事業の承継） | 事業譲渡・法人化でユーザー情報を承継させる同意が無かった。3項の**承継30日前の通知**は当社が追加（預かっているのが未使用コインと未払報酬であるため） | ⬜ **運用事項。** 承継が現実になった時点で通知を実施する |
| **第16条の4**（存続規定） | 退会・終了後も生き残る条項の明示が無かった | 実装不要 |
| **第16条の5**（分離可能性） | ★**当社にとって特に重要。** 複数の条項について「消費者契約法10条無効の典型的な標的」との指摘を受けているのに、1条が無効になったときの規定が無かった | 実装不要 |
| **第16条の6**（協議による解決） | — | 実装不要 |
| 第16条2項（管轄）の改訂 | 地方裁判所のみで、**少額の紛争を扱う簡易裁判所が書かれていなかった**（その範囲で合意管轄が働かない） | 実装不要 |

### 真似しなかったもの（GameRoom にあるが採用しない）

| GameRoom | 採用しない理由 |
|---|---|
| 第5条2項「**理由を問わず一切払い戻しを行いません**」 | 税理士Q14(c)が名指しした「一切の例外を認めない不返金条項は無効と判断されるリスク」 |
| 第5条3項「**売上coin も180日で失効**」 | 稼いだ報酬に期限。弁護士の「換金できない残高を人質にする外形は、優越的地位の濫用の評価において最も分の悪い事実」 |
| 退会条項2項「退会時に**払い戻し・返還はできない**」 | 売上コインごと消える。当社は退会後90日換金（第6条の2第4項） |
| 第7条2項「清算金は**当社が独自に定める方法により算定**」 | 料率が規約に無い。弁護士の言う「青天井」 |
| 第9条 終了「**事前に通知するよう努める**」＋残高の返金規定なし | 弁護士 論点8(d) の指摘そのもの。当社は90日前告知＋金銭返金（第13条2〜5項） |
| 第14条7項「**損害が発生した月に受領した利用額を限度**」 | 上限が0円になり得る。当社は総評5を受けて「1万円との高い方」に改訂済み |
| 第11条2項「**無償かつ無制限**に利用できる権利」の許諾 | 用途を限定しない包括許諾は消費者契約法10条の議論を招く。当社は「本サービスの提供・運営・改善および紹介に必要な範囲」に限定 |

> **他社規約は「最低限これは要る」の参考にはなっても、「これで足りる」の証明にはなりません。**
> 弁護士が指摘した3つの欠落（退会条項／サービス終了時の取扱い／賠償上限が0円になり得る）は、
> いずれも**同種サービスの規約にも無いもの**でした。他社に合わせていたら今も無いままです。
