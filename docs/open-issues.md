# 残論点の一覧(2026-07-26 時点)

公開までに残っている論点を、**着手の順番が分かる形**で1枚にまとめたものです。
細部の手順は `launch-checklist.md`、法務の詳細は `docs/legal/` を参照してください。

## いま全体を止めているもの

止まっている論点は実質**2つ**で、そこから多くの項目がぶら下がっています。

| # | ボトルネック | これが決まらないと進まないもの |
|---|---|---|
| **1** | **独自ドメインが未取得** | 特商法の販売URL / Supabaseのリダイレクト設定 / Stripeの `APP_URL` と本番Webhook / 規約の制定 |
| **2** | **弁護士のドラフト文言査読が未送付** | 規約4点の確定 / 事業者情報の記入 / 換金機能の解禁 / コイン販売の開始可否 |

どちらも**こちら側の作業ではなく、ご本人の作業**です。逆にいえば、この2つを
動かせば残りは一気に片付きます。ドメインは取得するだけ、査読は
`docs/legal/lawyer-review-round2-request.md` を送るだけの状態になっています。

---

## A. ホスティング

| 状態 | 項目 | 備考 |
|---|---|---|
| ✅ | Cloudflare への自動デプロイ | `main` にマージすれば反映される(`wrangler.jsonc` の静的配信) |
| ⬜ | **独自ドメインの取得・接続** | 上記ボトルネック1。特商法の販売URLに載るため早めに確定が必要 |
| ⬜ | スマホ実機での PWA 動作確認 | 「ホーム画面に追加」でアイコン・起動を確認 |

> `launch-checklist.md` のA項は Vercel を前提に書かれていましたが、実際には
> Cloudflare で運用しています(2026-07-26に追記して整合させました)。

## B. Supabase 本番設定

| 状態 | 項目 | 備考 |
|---|---|---|
| ✅ | マイグレーション 0001〜0029 の適用 | 2026-07-25 完了 |
| ✅ | Edge Function のデプロイ | avatar/voice/record-ip/Stripe系。「Verify JWT with legacy secret」はオフで運用 |
| ⬜ | **メール確認(Confirm email)を ON に戻す** | テストでOFFにしている場合。公開前必須 |
| ⬜ | リダイレクトURLの登録 | ドメイン待ち(ボトルネック1) |
| ⬜ | `pg_cron` の有効確認 | プレイ完了の72時間自動確定・コイン失効の日次ジョブに使用 |
| ⬜ | (推奨)バックアップ設定・ストレージポリシーの再確認 | |

## C. Stripe 本番化

| 状態 | 項目 | 備考 |
|---|---|---|
| ⬜ | **本番利用申請** | 事業内容は「非出会い系であることが伝わる説明」にする(審査対策) |
| ⬜ | 本番APIキー・`APP_URL` の設定 | ドメイン待ち |
| ⬜ | 本番Webhook の登録 | ドメイン待ち |
| ⬜ | 少額(¥300)の実購入テスト | |

> 申請には規約・特商法表記のURLを添えるため、**Dの法務とドメインが先**です。

## D. 法務ゲート

### D-1. 弁護士レビュー

| 状態 | 項目 |
|---|---|
| ✅ | 構造面の論点(Q1〜Q11)…収納代行/コイン有効期限/ギフトの適法性など。指摘はすべて規約・実装に反映済み |
| ⬜ | **ドラフト文言の査読(Q12〜Q19)を送付** …ボトルネック2。`lawyer-review-round2-request.md` を渡すだけ |
| ⬜ | 条文修正案(A〜D)の妥当性確認 …`lawyer-review-round2-amendments.md` を同時に提示 |

送付待ちの質問は次の8つです。

- Q12 受領権限授与の条文 / Q13 コイン有効期限 / Q14 直前キャンセルと消費者契約法9条
- Q15 ギフト条項 / Q16 プライバシーの3点 / Q17 住所・電話の請求時開示
- Q18 返金コインの期限起算(**実装修正を伴う**) / Q19 みまもり同意と通信の秘密

### D-2. 事業者情報の確定

回答待ちのため**まだ埋めてはいけない**項目です(`RELEASE-fill-in.md`)。

| 項目 | 待っているもの |
|---|---|
| 氏名・問い合わせメール | (いつでも記入可) |
| 販売URL | ドメイン(ボトルネック1) |
| 所在地・電話番号 | Q17の回答。バーチャルオフィスを使うかの事業判断も含む |
| 制定日 | 弁護士レビューの完了日 |

### D-3. ご本人の手続き

| 状態 | 項目 | 順序 |
|---|---|---|
| ⬜ | **分別管理用の専用口座の開設**(弁護士Q2(c)で強く推奨された) | ⚠️ **Q17の回答後**に行う(下記) |
| ⬜ | **税理士への確認**(支払調書・プラットフォーム情報報告制度・インボイス) | 先行して問題なし |

> ⚠️ **口座開設はQ17(住所方針)の回答を待ってから**にしてください。
> Q17でバーチャルオフィスを使う判断になった場合、その住所は特商法表記だけでなく
> **開業届・銀行口座・Stripe審査の登録住所**にも波及します。先に自宅住所で口座を
> 開設すると、後から住所を揃えたくなったときに手戻りが出ます。
> 税理士相談は住所に依存しないため、先に進めて構いません。

### D-4. 「資金決済法に基づく表示」ドラフトの扱い

コインの有効期限を6か月未満にして**適用除外ルートを採った**ため、前払式支払手段
としての**法定表示義務はありません**。にもかかわらず現在、`src/content/legalDocs.ts`
に法定表示の体裁のページが残っています。

このままだと「前払式支払手段の発行者である」と自認するような外観になり、かえって
紛らわしい状態です。次のどちらかに整理する必要があります。

- 「コインに関するご案内」等の**任意の説明ページに改組**する(有効期限・払戻不可・
  換金の条件などをユーザー向けに説明する内容として残す)
- **削除**して、必要な内容は規約第7条と特商法表記に寄せる

どちらが良いかは **Q13の枝問として弁護士に確認**します。

### D-5. 外部送信規律(電気通信事業法)

**棚卸しの結果(2026-07-26 実施)**: アクセス解析・エラー計測の類は**一切入って
いません**。`package.json` の依存は `@supabase/supabase-js` / `react` / `react-dom`
の3つだけで、GA・Sentry・PostHog等のコードもありません。

ただし **Google Fonts を外部から読み込んでいます**(`index.html` の
`fonts.googleapis.com` / `fonts.gstatic.com`)。フォント取得のたびに利用者の
IPアドレスとUser-Agentが Google に送信されるため、外部送信規律の検討対象に
なりえます。

対応は2通りあります。

- **フォントを自己ホストする**(`public/` に置いて `@font-face` で読む)。
  外部送信そのものが無くなるので、規律の検討が不要になる。**技術的にはこちらが簡単で確実**
- 外部送信について**公表・通知**を行う(プライバシーポリシーに記載を追加する)

自己ホストなら法務論点を1つ消せるので、そちらを推奨します。ご指示いただければ
対応します。なお Supabase への通信は本サービスの提供そのものに必要な送信のため、
別扱いになると考えられます(この整理の当否は Q20 で確認)。

---

## E. 実装の宿題(弁護士対応から出てきたもの)

いずれもDBの変更を伴うため、着手前に優先順位のご判断が必要です。

| # | 内容 | 由来 | 依存 |
|---|---|---|---|
| E-1 | 予約確定前にキャンセルポリシーを表示し**同意ログを残す** | Q14-① | なし(すぐ着手可) |
| E-2 | 直前キャンセル枠が**その後埋まったかの記録** | Q14-③ | なし(すぐ着手可) |
| E-4 | みまもりの**同意日時・同意文言バージョンの保存** | Q16 | なし(すぐ着手可) |
| E-5 | みまもりの**撤回導線**(設定画面) | Q16 | 撤回条項の書き方はQ19の回答待ち |
| E-8 | **返金コインの有効期限を当初発行日基準に改める** | Q13 | マイグレーションを先に書いてホールド。適用はQ18の回答後 |
| E-9 | **カードフィンガープリントの監視**(購入フロー/Stripe webhookでの保存) | Q11-c | **依存: 換金解禁前までに完了が必要** |

> **E-4 が最優先**と考えます。みまもりの同意画面自体は実装済みですが、同意した
> 事実がどこにも記録されないため、「同意を取得している」ことを後から証明できません。
> 機能の前提に関わる欠落です。
>
> **E-8 は「修正するか」ではなく「修正方法が妥当か」の段階**です。適用除外の根拠が
> 「発行の日から6月内に限り使用できる」ことである以上、返金時に新期限を振り直す
> 現実装は除外要件を正面から割ります。Q18は当初発行日引き継ぎという**修正方法の
> 妥当性確認**として聞く位置づけに読み替え、マイグレーションは先に書いて回答待ちで
> ホールドします。
>
> **E-9 は「継続課題」ではなく換金解禁の前提条件**として扱います。換金🔒を外す
> タイミングまでに購入フロー側の保存実装が終わっている必要があるため、解禁直前に
> 慌てないようE群に移しました。

### E-8 の対象範囲について(ギフト受領コインは含まない)

「ギフトとして受領したコインも同じ起算問題を持つのでは」という点を実装で確認した
ところ、**当実装では発生しません**でした。

`send_gift`(0020)の受領側の処理は `earned_balance` への加算だけで、
`coin_lots`(発行日と期限を持つロット)は作られません。`earned_balance` の減少経路は
換金申請のみで、サービス内の支払いには使えないため、前払式支払手段としての期限管理
の対象外です。したがって E-8 の修正スコープは**返金(再付与)経路のみ**です。

> ⚠️ ただし、確認の過程で**別の不一致**が見つかりました。規約第7条5項は
> 「**コイン**の有効期限は取得日から起算して6か月」と書いており、文言上は第7条6項の
> **報酬コインも含まれてしまいます**。一方で実装上、報酬コイン(`earned_balance`)には
> 有効期限がありません。実装のほうがユーザーに有利ではあるものの、**規約と実装が
> 食い違っている**ので、条文側で「報酬コインには適用しない」と書き分ける必要が
> あります。Q18の枝問として確認します。

## F. 継続課題(前回のレビューから持ち越し)

| 状態 | 項目 |
|---|---|
| ⬜ | ギフトの**為替該当性**の最終評価(実装を作り替えた後の設計で再確認する) |
| 🔒 | **換金(ホストへの振込)は弁護士OKまで実行しない** …申請は溜めてよいが、総合振込を実行しない限り資金移動は発生しない |
| 🔒 | オフライン会場提供への事業拡大はしない(風営法リスクが一変する) |
| 🔒 | 検索・レコメンドで異性を優先表示しない / 金額ベースのランキングを作らない |

---

## 推奨する進め方

1. **ドメインを取得する**(ボトルネック1が外れ、B・C・Dの複数項目が動く)
2. **弁護士に `lawyer-review-round2-request.md` と `-amendments.md` を送る**(ボトルネック2)
3. 待っている間に **E-1・E-2・E-4** を実装する(回答に依存しない)
4. 並行して**専用口座の開設**と**税理士への相談**を進める
5. 回答が来たら、条文へ反映 → E-5・E-8 を実装 → 事業者情報を記入 → 制定日を入れる
6. 最後に **Stripe本番化**と**実機の通し確認**(登録→本人確認→購入→予約→トーク→完了→レビュー)
